# 发布检查清单

发布前逐项检查。

## 本地文件

- `README.md` 能说明项目用途。
- `docs/01-快速使用.md` 步骤可照着做。
- `docs/02-原理与试错路径.md` 说明最终方案和失败路径。
- `docs/03-备份恢复与修改范围.md` 清楚说明会改什么。
- `NOTICE.md` 说明 DLSSTweaks 来源。
- `.gitignore` 排除本机备份、缓存和构建产物。

## 功能验证

```powershell
python -m py_compile app.py
```

手动验证：

- `run.bat` 能启动。
- 首页能打开。
- `/api/state` 正常。
- 游戏路径检测正常。
- 安装前会创建备份。
- 恢复会按 manifest 回滚。
- HUD 状态能读取。

## 仓库发布

- 不上传 `_local_backups`。
- 不上传 `__pycache__`。
- 不上传 `dist`，除非是在 GitHub Release 里单独上传打包 zip。
- 如果包含 DLSSTweaks binary，保留 `NOTICE.md` 和 `assets/dlsstweaks/README.md`。

## Release 文案模板

```text
NTE DLSS Panel v0.1.0

Local WebUI for applying the tested DLSSTweaks winmm.dll path to Neverness To Everness.

Highlights:
- One-click game path detection.
- Manifest-based backup and restore.
- DLSS ratio editor below 33%.
- NVIDIA DLSS HUD toggle.

Recommended path:
- Select game root.
- Use 0.30 or 0.315.
- Keep all four DLSS levels synced.
- Restart the game.
```
